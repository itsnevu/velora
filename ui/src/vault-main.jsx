import React from 'react'
import ReactDOM from 'react-dom/client'
import VaultApp from './vault-app.jsx'

ReactDOM.createRoot(document.getElementById('vault-root')).render(
  <React.StrictMode>
    <VaultApp />
  </React.StrictMode>,
)
